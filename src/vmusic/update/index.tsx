// Silent background component.
// The main process triggers update checks automatically via did-finish-load.
// All status updates flow through "update-status" IPC → TitleBar state machine.
const Update = () => null;

export default Update;
